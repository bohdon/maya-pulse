RenameSceneAction:
  id: Pulse.RenameScene
  displayName: Rename Scene
  description: Renames the scene
  category: Core
  attrs:
    - name: filename
      type: string
      description: The new scene name, can contain format specifiers that
        accept the rig's meta data.
      value: "{rig[name]}_rig"